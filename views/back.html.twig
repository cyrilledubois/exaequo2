{% extends "layout.html.twig" %}

  {% block title %}Back_user{% endblock %}

  {% block content %}

<body id="page-top">
  <header id="headuser" class="bg-primary text-white">
    <div class="container text-center">
      <h1>Bienvenue dans votre interface d'administration</h1>
    </div>
  </header>

  <nav id="navuser" class="navbar navbar-default">
    <div class="navbar-header">
      <ul id="uluser" class="navbar-nav ml-auto">
        <li class="nav-item">
          <a class="nav-link js-scroll-trigger" href="#planning">Mes Cours</a>
        </li>

        <li class="nav-item">
          <a class="nav-link js-scroll-trigger" href="#abo">Mes Abonnements</a>
        </li>

        <li class="nav-item">
          <a class="nav-link js-scroll-trigger" href="#info">Mes Infos</a>
        </li>

      </ul>
    </div>
  </nav>
{# Ajout info utilisateur #}

<div class="container">
<div class="col-lg-8 col-md-8 col-sm-7 col-xs-7" >

    
    {{ form_start(userForm, { 'attr': {'class': 'form-horizontal'} }) }}
      <div class="form-group">
        {{ form_label(userForm.email, null) }}
        <div class='form-input'>
          {{ form_widget(userForm.email, { 'attr':  {
            'class': 'form-control'
          }}) }}
          {{ form_errors(userForm.email) }}
        </div>
      </div>
      
      <div class="form-group">
        {{ form_label(userForm.username, null) }}
        <div class='form-input'>
          
          {{ form_widget(userForm.username, { 'attr':  {
            'class': 'form-control'
          }}) }}
          {{ form_errors(userForm.username) }}
        </div>
      </div>

      <div class="form-group">
        {{ form_label(userForm.firstname, null) }}
        <div class='form-textarea'>
        
          {{ form_widget(userForm.firstname, { 'attr':  {
            'class': 'form-control'
          }}) }}
          {{ form_errors(userForm.firstname) }}
        </div>
      </div>
      <div class="form-group">
        {{ form_label(userForm.adress, null) }}
        <div class='form-input'>
          
          {{ form_widget(userForm.adress, { 'attr':  {
            'class': 'form-control'
          }}) }}
          {{ form_errors(userForm.adress) }}
        </div>
      </div>

      <div class="form-group">
        {{ form_label(userForm.compadress, null) }}
        <div class='form-input'>
          
          {{ form_widget(userForm.compadress, { 'attr':  {
            'class': 'form-control'
          }}) }}
          {{ form_errors(userForm.compadress) }}
        </div>
      </div>

      <div class="form-group">
        {{ form_label(userForm.cp, null) }}
        <div class='form-input'>
          
          {{ form_widget(userForm.cp, { 'attr':  {
            'class': 'form-control'
          }}) }}
          {{ form_errors(userForm.cp) }}
        </div>
      </div>

      <div class="form-group">
        {{ form_label(userForm.datedenaissance, null) }}
        <div class='form-input'>
          
          {{ form_widget(userForm.datedenaissance, { 'attr':  {
            'class': 'form-control'
          }}) }}
          {{ form_errors(userForm.datedenaissance) }}
        </div>
      </div>

      <div class="form-group">
        {{ form_label(userForm.groupeclient, null) }}
        <div class='form-input'>
          
          {{ form_widget(userForm.groupeclient, { 'attr':  {
            'class': 'form-control'
          }}) }}
          {{ form_errors(userForm.groupeclient) }}
        </div>
      </div>
      
      <div class="form-group">
        {{ form_label(userForm.password.first, null) }}
        <div class='form-input'>
          
          {{ form_widget(userForm.password.first, { 'attr':  {
            'class': 'form-control'
          }}) }}
          {{ form_errors(userForm.password.first) }}
        </div>
      </div>


      <div class="form-group">
        {{ form_label(userForm.password.second, null) }}
        <div class='form-input'>
          
          {{ form_widget(userForm.password.second, { 'attr':  {
            'class': 'form-control'
          }}) }}
          {{ form_errors(userForm.password.second) }}
        </div>
      </div>
      
      <div class="form-group">
        {{ form_label(userForm.phone, null) }}
        <div class='form-input'>
          
          {{ form_widget(userForm.phone, { 'attr':  {
            'class': 'form-control'
          }}) }}
          {{ form_errors(userForm.phone) }}
        </div>
      </div>

      <div class="form-group">
        {{ form_label(userForm.profession, null) }}
        <div class='form-input'>
          
          {{ form_widget(userForm.profession, { 'attr':  {
            'class': 'form-control'
          }}) }}
          {{ form_errors(userForm.profession) }}
        </div>
      </div>

      <div class="form-group">
        {{ form_label(userForm.sex, null) }}
        <div class='form-input'>
          
          {{ form_widget(userForm.sex, { 'attr':  {
            'class': 'form-control'
          }}) }}
          {{ form_errors(userForm.sex) }}
        </div>
      </div>

      <div class="form-group">
        {{ form_label(userForm.town, null) }}
        <div class='form-input'>
          
          {{ form_widget(userForm.town, { 'attr':  {
            'class': 'form-control'
          }}) }}
          {{ form_errors(userForm.town) }}
        </div>
      </div>

      <div class="form-group">
        <input type="submit" class="btn btn-primary btn-lg" name="submit" value="Send" />
      </div>
    {{ form_end(userForm) }}
  </div>

  </div>
 

{% endblock %}